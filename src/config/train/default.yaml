output_root: runs
leaderboard_csv: ${hydra:runtime.cwd}/reports/benchmark_results.csv
num_labels: 2
learning_rate: 2e-5
epochs: 3
train_bs: 8 # bs = batch_size
eval_bs: 16
weight_decay: 0.01
eval_strategy: epoch
save_strategy: epoch
logging_steps: 50
metric_for_best_model: f1
fp16: true
report_to: none 