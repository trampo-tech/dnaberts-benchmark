[ ] Run evo2 model on gue tasks
[ ] Figure out if token max_length needs to change for evo2 models (possibly so, need to figure out how to change it)
[ ] Remove model fallback, its remnant of some model we tested (dnabert1 possibly)